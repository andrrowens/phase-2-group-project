import React from 'react'

const Answers = ({answers}) => {
  // const [currentQuestion, setCurrentQuestion] = useState(0);
 
  // const handleClick = (e) => {
  //   setCurrentQuestion(item.isCorrect)}

  
  return (
   <div>
    <button>Answers </button>
    <button>Answers </button>
    <button>Answers </button>
    <button>Answers </button>
   </div>
  )
            }
export default Answers
